import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sagarkolte.nyccommuteweb',
  appName: 'NYC Commute',
  webDir: 'out'
};

export default config;
